# Systar 前端交互测试清单

> 版本：v1.2 | 更新：2026-05-29
> 测试环境：Vite dev server (5173) + Systar(8081) + H2

## 测试结果摘要（2026-05-29 第三轮）

| 编号 | 测试项 | 结果 |
|------|--------|------|
| TC-AUTH-01 | 登录成功 | ✅ PASS |
| TC-AUTH-06 | 退出登录 | ✅ PASS |
| TC-AUTH-07 | 404 路由 | ✅ PASS |
| TC-AUTH-08 | 会话过期处理 | ✅ PASS |
| TC-DASH-01 | Dashboard 数据加载 | ✅ PASS（6统计卡片+LIVE标记） |
| TC-DASH-02 | ECharts 渲染 | ✅ PASS（5图表区域：资产状态/告警趋势/工单状态/Top告警/在线率） |
| TC-DASH-06 | 告警概览 | ✅ PASS（今日告警0+待处理0） |
| TC-ASSET-01 | 资产树加载 | ✅ PASS（39节点完整层级树） |
| TC-MON-01 | 监控页面 | ✅ PASS（0 errors） |
| TC-ALARM-01 | 告警规则 | ✅ PASS（7条规则，列完整：ID/监测器ID/策略/通知方式/级别/触发次数/模板/状态） |
| TC-CTRL-01 | 控制器树 | ✅ PASS（0 errors） |
| TC-LINK-01 | 联动规则 | ✅ PASS（1条规则"消防报警联动"+展开行显示触发条件和执行动作） |
| TC-UI-01 | 全页面零 JS 错误 | ✅ PASS（遍历所有页面0 JS error） |
| TC-UI-02 | 导航高亮 | ✅ PASS |
| TC-UI-03 | Username 持久化 | ✅ PASS（显示admin） |

### 新增页面测试（2026-05-29）

| 页面 | 路由 | 结果 |
|------|------|------|
| 工单管理 | /workorder | ✅ PASS（V1.0标题+4统计卡片+列表/统计Tab+筛选+分页，0 errors） |
| 设备台账 | /ledger | ✅ PASS（0 errors） |
| 巡检管理 | /inspection | ✅ PASS（0 errors） |
| 统计报表 | /statistics | ✅ PASS（V2.0标题+7侧边栏+日期范围+告警概览卡片，0 errors） |
| 定时任务 | /schedule | ✅ PASS（详见schedule-interaction-test.md） |
| 资产运维 | /operations | ✅ PASS（详见operations-interaction-test.md） |

## 1. 认证与路由

### TC-AUTH-01 登录成功
- 前置：未登录状态
- 步骤：输入 admin / admin123 → 点击"登录"
- 预期：跳转到 /dashboard，头部显示用户名

### TC-AUTH-02 登录失败（错误密码）
- 步骤：输入 admin / wrongpassword → 点击"登录"
- 预期：页面停留在 /login，显示错误提示消息

### TC-AUTH-03 登录表单验证
- 步骤：清空用户名 → 触发 blur
- 预期：显示"请输入用户名"内联错误

### TC-AUTH-04 已登录访问 /login 自动跳转
- 前置：已登录
- 步骤：导航到 /login
- 预期：自动跳转到 /dashboard

### TC-AUTH-05 未登录访问受保护页面
- 前置：未登录（清除 token）
- 步骤：导航到 /dashboard
- 预期：跳转到 /login

### TC-AUTH-06 退出登录
- 步骤：点击右上角"退出"按钮
- 预期：token 清除，跳转到 /login

### TC-AUTH-07 404 路由
- 步骤：导航到 /nonexistent-page
- 预期：重定向到 /dashboard

### TC-AUTH-08 会话过期处理
- 步骤：token 过期后触发 API 调用
- 预期：弹出"登录状态已过期"对话框，点击"重新登录"跳转到 /login

## 2. Dashboard 监控大屏

### TC-DASH-01 数据加载
- 预期：4 个统计卡片显示正确数值（设备总数、在线设备、告警总数、监测点）

### TC-DASH-02 ECharts 渲染
- 预期：系统状态分布饼图渲染，LIVE 标记显示

### TC-DASH-03 数值动画
- 预期：统计数字从 0 动画递增到目标值（800ms 缓动）

### TC-DASH-04 错误重试
- 步骤：后端不可用时点击"重试"按钮
- 预期：错误横幅显示，点击重试重新请求

### TC-DASH-05 窗口缩放
- 步骤：调整浏览器窗口大小
- 预期：ECharts 图表自适应

### TC-DASH-06 告警概览
- 预期：显示待处理和总计告警数；无告警时显示"暂无待处理告警"

### TC-DASH-07 无告警状态
- 预期：pendingAlarms === 0 时显示 el-empty

## 3. 资产管理

### TC-ASSET-01 资产树加载
- 预期：左侧 TreePanel 显示完整层级树（空间→设备→监测器/控制器）

### TC-ASSET-02 树搜索过滤
- 步骤：在搜索框输入关键字
- 预期：树节点按名称过滤

### TC-ASSET-03 树展开/收起全部
- 步骤：点击展开/收起图标
- 预期：所有节点展开或收起

### TC-ASSET-04 树刷新
- 步骤：点击刷新图标
- 预期：重新请求树数据

### TC-ASSET-05 树侧边栏折叠
- 步骤：点击折叠按钮
- 预期：侧边栏折叠到最小宽度，再点展开

### TC-ASSET-06 树侧边栏拖拽调整宽度
- 步骤：拖拽右侧手柄
- 预期：宽度在 180-400px 范围内变化，松手后保存到 localStorage

### TC-ASSET-07 点击树节点筛选表格
- 步骤：点击树中的节点（如"设备"节点）
- 预期：右侧表格按该类型过滤

### TC-ASSET-08 类型下拉筛选
- 步骤：选择资产类型 → 点击"搜索"
- 预期：表格按类型过滤

### TC-ASSET-09 重置筛选
- 步骤：点击"重置"
- 预期：类型清空，表格显示全部

### TC-ASSET-10 表格数据完整性
- 预期：每行显示 ID、名称、标题、类型(tag)、状态(tag)、路径、启用(tag)

### TC-ASSET-11 查看资产详情
- 步骤：选中一行 → 点击"详情"
- 预期：弹出详情对话框，显示完整属性

### TC-ASSET-12 未选中点击详情
- 步骤：不选中任何行 → 点击"详情"
- 预期：提示"请选择要查看的资产"

## 4. 实时监控

### TC-MON-01 资产树加载
- 预期：树结构正确显示（与资产管理一致）

### TC-MON-02 点击 Probe 节点加载数据
- 步骤：点击树中的 Probe 类型节点
- 预期：右侧表格显示该 Probe 的当前值

### TC-MON-03 点击父节点加载所有子 Probe
- 步骤：点击设备或空间节点
- 预期：收集所有子 Probe ID，批量查询并显示

### TC-MON-04 手动输入 Probe ID 查询
- 步骤：在输入框输入逗号分隔的 ID → 点击查询
- 预期：显示对应 Probe 数据

### TC-MON-05 自动刷新
- 步骤：开启自动刷新开关
- 预期：每 5 秒自动刷新数据；关闭开关后停止

### TC-MON-06 查看历史数据
- 步骤：点击某行的"历史"按钮
- 预期：下方显示历史面板，包含时间范围选择器、折线图、数据表格

### TC-MON-07 历史时间范围查询
- 步骤：选择时间范围 → 点击查询
- 预期：折线图和数据表格更新

### TC-MON-08 历史分页
- 预期：分页组件正确工作，切换页码加载数据

### TC-MON-09 图表自适应
- 步骤：调整窗口大小
- 预期：ECharts 折线图自适应

## 5. 告警管理

### TC-ALARM-01 告警规则 Tab
- 步骤：切换到"告警规则"tab
- 预期：表格显示规则列表（ID、监测器ID、策略、方式、触发值、消息模板、启用状态）

### TC-ALARM-02 告警消息 Tab
- 步骤：切换到"告警消息"tab
- 预期：表格显示消息列表，带分页

### TC-ALARM-03 按处理状态过滤
- 步骤：选择"处理状态"下拉（待处理/已处理）
- 预期：消息列表按状态过滤，分页重置

### TC-ALARM-04 按恢复状态过滤
- 步骤：选择"恢复状态"下拉（未恢复/已恢复）
- 预期：消息列表按恢复状态过滤

### TC-ALARM-05 告警消息分页
- 预期：分页组件正确工作

### TC-ALARM-06 刷新按钮
- 步骤：点击刷新
- 预期：重新加载当前 tab 数据

## 6. 控制执行

### TC-CTRL-01 控制器树加载
- 预期：树结构显示控制器节点

### TC-CTRL-02 树搜索过滤
- 步骤：输入关键字
- 预期：树节点按名称过滤

### TC-CTRL-03 选择控制器
- 步骤：点击 CONTROL 类型节点
- 预期：右侧面板显示控制器详情（ID、名称、状态），命令输入框

### TC-CTRL-04 点击非控制器节点
- 步骤：点击 DEVICE 类型节点
- 预期：面板不切换（只响应 CONTROL 类型）

### TC-CTRL-05 未选择时显示提示
- 预期：面板显示 el-empty "请从左侧选择一个控制器"

### TC-CTRL-06 执行命令（成功）
- 步骤：选择控制器 → 输入命令 → 点击"执行" → 确认
- 预期：弹出确认对话框 → 成功后显示"命令执行成功"，历史记录新增一条

### TC-CTRL-07 执行命令（失败）
- 预期：失败时显示错误消息，但历史仍记录（含失败状态）

### TC-CTRL-08 空命令验证
- 步骤：不输入命令直接点执行
- 预期：提示"请选择控制器并输入命令"

### TC-CTRL-09 历史记录上限
- 预期：历史最多保留 50 条，超出后删除最旧记录

### TC-CTRL-10 Enter 键快捷执行
- 步骤：在命令输入框按 Enter
- 预期：触发执行流程

## 7. 联动规则

### TC-LINK-01 规则列表加载
- 预期：表格显示联动规则（名称、触发类型、启用状态、操作按钮）

### TC-LINK-02 触发类型标签
- 预期：MONITOR 类型显示蓝色标签，ALARM 类型显示橙色标签

### TC-LINK-03 新增联动规则
- 步骤：点击"新增" → 填写规则名称 → 选择触发类型(MONITOR/ALARM) → 从原因树选择监测器 → 设置触发值 → 从效果树选择控制器 → 输入命令 → 保存
- 预期：规则创建成功，列表刷新显示新规则

### TC-LINK-04 编辑联动规则
- 步骤：点击"编辑" → 修改名称/触发类型/原因/效果 → 保存
- 预期：规则更新成功

### TC-LINK-05 删除联动规则
- 步骤：点击"删除" → 确认
- 预期：规则及关联的 cause/effect 级联删除

### TC-LINK-06 启用/禁用切换
- 步骤：点击启用状态开关
- 预期：规则启用状态切换，联动引擎实时生效

### TC-LINK-07 双树面板交互
- 预期：左侧原因树（监测器）和右侧效果树（控制器）独立展示，选中节点显示详情

### TC-LINK-08 空命令验证
- 步骤：新增规则时不选择原因或效果 → 保存
- 预期：提示必填项错误

### TC-LINK-09 无规则时显示空状态
- 预期：表格显示 el-empty "暂无联动规则"

### TC-LINK-10 刷新按钮
- 步骤：点击刷新
- 预期：重新加载规则数据

## 8. 系统管理

### TC-SYS-01 系统管理页面加载
- 步骤：访问系统管理各页面（用户/角色/菜单/部门/日志/通知）
- 预期：页面加载正常，console 无 error

## 9. 通用 UI 检查

### TC-UI-01 所有页面零 JS 错误
- 步骤：遍历所有页面
- 预期：console 无 error 级别日志

### TC-UI-02 侧边栏导航高亮
- 步骤：在各页面间切换
- 预期：当前页面的导航链接显示 active 状态

### TC-UI-03 权限指令
- 步骤：检查 v-hasPermi 指令在无权限时隐藏按钮
- 预期：按钮正确显示/隐藏

### TC-UI-04 请求错误提示
- 步骤：后端不可用时触发 API 调用
- 预期：显示 ElMessage 错误提示（非 401 的业务错误）

### TC-UI-05 网络错误提示
- 步骤：后端完全不可达时触发 API 调用
- 预期：显示"后端服务连接异常"

### TC-UI-06 超时提示
- 预期：请求超时时显示"请求超时"

## 10. 数据流验证

### TC-DATA-01 Dashboard 数据流
- 验证链路：前端 → systarApi GET /api/monitor/dashboard → Systar 后端
- 预期：响应 data 正确渲染到统计卡片与图表

### TC-DATA-02 分页数据 key 一致性
- 验证：Systar 返回 `records`（MyBatis-Plus Page）→ 前端表格直接读取 `records`
- 预期：分页数据正确传递，总数与页码一致

### TC-DATA-03 错误码处理
- 验证：code=0 正常返回；code=401 触发重新登录提示；其他非 0 code 弹出错误消息
- 预期：三类响应均按拦截器规则处理
